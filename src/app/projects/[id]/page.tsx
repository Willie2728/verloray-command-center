import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { getProject } from "@/lib/store";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){const project=await getProject((await params).id);if(!project)notFound();return <AppShell><div className="page-wrap"><header className="page-head"><span className="eyebrow">PROJECT MEMORY</span><h1>{project.name}</h1><p>{project.description}</p></header>{project.conversations.map(c=><article className="info-card" key={c.id} style={{marginBottom:13}}><span className="tag">{c.providerIds.join(" · ")}</span><h3>{c.title}</h3>{c.messages.map(m=><div className="project-conversation" key={m.id}><b>{m.role === "user" ? "You" : m.providerId}</b><p style={{whiteSpace:"pre-wrap"}}>{m.content}</p></div>)}</article>)}</div></AppShell>}
